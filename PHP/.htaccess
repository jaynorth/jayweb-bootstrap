# .htaccess mod_rewrite
<IfModule mod_rewrite.c>

#Turn on the engine
RewriteEngine on

#RewriteBase /modules/

#Redirect certain paths to index.php
RewriteRule ^(photo|dev|contact|temperature|electrovintage|vinyl)/?$ index.php?p=$1

</IfModule>